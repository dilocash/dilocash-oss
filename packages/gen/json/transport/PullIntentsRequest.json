{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$ref": "#/definitions/PullIntentsRequest",
    "definitions": {
        "PullIntentsRequest": {
            "properties": {
                "last_updated_at": {
                    "type": "string",
                    "description": "The checkpoint that RxDB will send",
                    "format": "date-time"
                },
                "limit": {
                    "type": "integer",
                    "description": "Batch size"
                }
            },
            "additionalProperties": true,
            "type": "object",
            "title": "Pull Intents Request",
            "description": "request to pull changes from the server"
        }
    }
}