# Copyright (c) 2026 dilocash
# Use of this source code is governed by an MIT-style
# license that can be found in the LICENSE file.

version: "2"
sql:
  - schema: "schema.sql"
    queries: "queries.sql"
    engine: "postgresql"
    gen:
      go:
        package: "postgres"
        out: "../../apps/api/internal/generated/db/postgres"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false
        overrides:
          - db_type: "uuid"
            go_type: "github.com/google/uuid.UUID"

          - column: "commands.created_at"
            go_type: "time.Time"
          - column: "commands.updated_at"
            go_type: "time.Time"
          - column: "commands.deleted"
            go_type: "bool"

          - column: "transactions.amount"
            go_type: "github.com/shopspring/decimal.Decimal"
          - column: "transactions.created_at"
            go_type: "time.Time"
          - column: "transactions.updated_at"
            go_type: "time.Time"
          - column: "transactions.deleted"
            go_type: "bool"

          - column: "intents.created_at"
            go_type: "time.Time"
          - column: "intents.updated_at"
            go_type: "time.Time"
          - column: "intents.deleted"
            go_type: "bool"

          - column: "profiles.created_at"
            go_type: "time.Time"
          - column: "profiles.accepted_terms_at"
            go_type: "time.Time"
          - column: "profiles.allow_data_analysis"
            go_type: "bool"
overrides:
  go:
    rename:
      ID: Id
  # --- Future MySQL Configuration ---
  # - schema: "mysql/schema.sql"
  #   queries: "mysql/queries.sql"
  #   engine: "mysql"
  #   gen:
  #     go:
  #       package: "mysql"
  #       out: "../../apps/api/internal/database/mysql"
  # ... MySQL specific settings
